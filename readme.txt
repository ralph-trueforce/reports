To run the server follow these steps:

1. Be sure to have node.js installed
2. Be sure to have node-mysql for node.js
         mkdir sp-node-mysql
         cd sp-node-mysql
         npm install mysql
3. Have the 'reports' database imported.
4. Run the next lines in the shell (need to be in the root directory)
         $ node node.js/server.js
